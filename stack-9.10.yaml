resolver: nightly-2025-02-24

packages:
  - core
  - hunit
  - quickcheck2
  - example

ghc-options:
  $locals: >-
    -Wall
    -Wcompat
    -Wno-deprecations
    -Wno-x-partial
  # This package is legacy and won't address deprecation warnings.
